{
  "version": 2,
  "builds": [
    {
      "src": "api/remove-bg.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ]
}
